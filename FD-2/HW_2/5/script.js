'use strict'
var num = Number(prompt('How many people were there: ', 10));
for (var x = 1; x <= num; x++) {
  var age = Number(prompt('The age of the ' + x + ' ' + 'visitor: ', 18));
}
var min;
var max;
var avg;
